apiVersion: '1'
id: 'non-cap-plugin'
description: 'testing plugin catalog for non-cap plugins'
availabilityPattern: '.*'
version: '1'

parent: 'default-global'
plugins:
  - 'plugins.yaml'
catalog:
  - 'plugin-catalog.yaml'
