apiVersion: '1'
id: 'casc-api-2'
description: 'testing sort of anonymous artifactory via plugin catalog'
availabilityPattern: '.*'
version: '1'

plugins:
  - 'plugins.yaml'
catalog:
  - 'plugin-catalog.yaml'
